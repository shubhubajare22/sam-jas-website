{
  "name": "samandjas-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sam and Jas — modern website redesign prototype. Zero runtime dependencies.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node serve.mjs",
    "serve": "node serve.mjs"
  },
  "engines": { "node": ">=18" }
}
